version: '2.1'

services:
  frontend:
    image: dreamsim-frontend
    environment:
      - NODE_ENV=production
      - API_URL='https://dreamsim'
    volumes:
      - .:/usr/src/app
    ports:
      - "80:80"

