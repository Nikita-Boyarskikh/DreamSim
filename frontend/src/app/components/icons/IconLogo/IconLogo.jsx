import React from 'react';
import BaseIcon from 'app/components/icons/BaseIcon';

import logo from 'app/components/icons/IconLogo/logo.svg';

const IconLogo = (props) => <BaseIcon icon={logo} {...props} />;

export default IconLogo;
